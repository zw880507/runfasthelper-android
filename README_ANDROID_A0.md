# A0 note
The original offline-video A0 code remains in `video/OfflineVideoAnalyzer.java` only as a diagnostic/reference utility.
The production entry point is now Android Live v0.9.0; see `README_ANDROID_LIVE.md`.
